{
  "compilerOptions": {
    // Suas outras opções como module: "NodeNext", moduleResolution: "NodeNext", etc.
    "baseUrl": ".", // Necessário para 'paths'
    "paths": {
      "@prisma/client": ["./node_modules/.prisma/client"] // Mapeia o alias para o local gerado
    },
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
